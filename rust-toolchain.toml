# Pin to the Cargo.toml MSRV (1.85). The tree uses edition 2024, so older
# rustcs fail with an error that does not name the cause. Floating `stable`
# can also break clippy `-D warnings` without a lockfile change.
[toolchain]
channel = "1.85"
components = ["clippy", "rustfmt"]
